name: fete 
hp_name: ${name}-${env.hp_name}_s-${seed}

update_behavior_every: 100 # in terms of episodes
num_episodes: ${env.num_episodes}

weighting: True

defaults:
  - base
  - override model: fete_gpt2
  - override env: bandit
  - _self_