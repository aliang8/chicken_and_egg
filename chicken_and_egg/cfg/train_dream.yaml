name: dream
hp_name: ${name}_${env.hp_name}_s-${seed}

buffer:
  type: sequential
  size: 16000
  sequence_length: 50
  
num_train_envs: 1
num_eval_envs: 1

data:
  batch_size: 32

defaults:
  - base
  - override model: dream
  - override env: bandit
  - _self_