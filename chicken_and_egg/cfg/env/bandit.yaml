env_name: bandit_${env.bandit_type}
env_id: test
obs_dim: 10
act_dim: 10
timesteps_per_episode: 1
num_episodes: 10
bandit_type: reg
hp_name: bt-${env.bandit_type}

env_kwargs:
  n: 10
  noise_scale: 0.5
  minval: 0.5
